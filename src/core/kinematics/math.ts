/**
 * @deprecated Import from `../geometry/vector` instead. This file re-exports
 * for backward compatibility and will be removed in a future major version.
 */
export {
  add,
  angleBetween,
  cosineSimilarity,
  dot,
  norm,
  normalize,
  project,
  reject,
  scale,
  subtract,
  toMatrix,
  toVector,
} from '../geometry/vector';
